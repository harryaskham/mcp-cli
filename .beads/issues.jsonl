{"id":"bd-02792a","title":"Rework azure-ephemeral CI to use a Nix devshell for the toolchain","description":"Follow-up to bd-f023df. Operator (Harry / router, 2026-06-28): the azure-ephemeral runners have Nix+flakes but NO bare toolchains on PATH (bare cargo is absent). Agents hit missing-toolchain failures. Fix: rework CI to enter a Nix devshell (nix develop .#<shell>) for the toolchain instead of installing it via an action or requiring a custom runner image.\n\nFor mcp-cli (which currently provisions rust via dtolnay/rust-toolchain@stable and has no flake):\n1. Add a flake.nix exposing devShells.default with the Rust toolchain (cargo, rustc, clippy, rustfmt) from nixpkgs; commit the generated flake.lock for reproducibility.\n2. Rework .github/workflows/ci.yml: drop the dtolnay/rust-toolchain step; run each check under `nix develop --command cargo ...` (fmt --check, clippy -D warnings, test). Keep the job on runs-on [self-hosted, azure-ephemeral]; keep workflow_dispatch-only until mcp-cli is added to the runner pool repos list.\n\nValidate locally with Nix (available here): nix flake lock; nix develop --command cargo fmt/clippy/test all green. Also confirm plain cargo still passes.","status":"closed","priority":2,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-28T20:09:00.397908783Z","updated_at":"2026-06-28T20:18:28.961451202Z","closed_at":"2026-06-28T20:18:28.961450547Z","is_blocked":false,"spoken_name":"nix devshell ci toolchain","occurrence_count":1,"last_seen_at":"2026-06-28T20:09:00.397908783Z"}
{"id":"bd-04137b","title":"Cover read_protocol_message framing error paths and write_json_result with tests","description":"The MCP message framing reader `read_protocol_message` has three error/robustness\npaths with no test coverage:\n\n- missing Content-Length header -> McpCliError::Protocol(\"missing Content-Length ...\")\n- invalid Content-Length value (non-numeric) -> Protocol(\"invalid Content-Length ...\")\n- unexpected EOF after headers began but before a blank line -> Protocol(\"unexpected EOF ...\")\n\nAdditionally the owned `write_json_result` helper (as opposed to the already-used\n`write_json_result_ref`) has no direct test.\n\nThese guard the JSON-RPC stdio transport against malformed client framing producing\npanics instead of clean structured Protocol errors, and lock the owned envelope\nwriter's output shape.\n\nScope: test-only additions in src/lib.rs #[cfg(test)]. The framing reader is a\nprivate fn, so tests live in the same module (already the case). No production code\nchanges expected. Keep generic.\n\nFiled by mcp-cli-dev-aur-0 while progressing the lane.\n","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-04T23:53:32.058542702Z","updated_at":"2026-06-04T23:56:24.602605529Z","closed_at":"2026-06-04T23:56:24.602605129Z","is_blocked":false,"spoken_name":"test framing error paths","occurrence_count":1,"last_seen_at":"2026-06-04T23:53:32.058542702Z"}
{"id":"bd-189cfc","title":"Cover MCP ping, initialized-notification, and unknown-method (-32601) paths with tests","description":"The stdio MCP server (`McpServer::handle_request`) implements several JSON-RPC\nbehaviors that currently have no test coverage:\n\n- `ping` -> returns `{\"result\": {}}` with the request id.\n- `notifications/initialized` -> returns no response (notification; id-less).\n- unknown/unsupported method -> JSON-RPC error object with code `-32601` and an\n  \"unsupported MCP method\" message.\n\nOnly initialize/tools.list/tools.call are exercised today (via\nstdio_server_handles_initialize_list_and_call). Adding focused tests for ping,\nthe initialized notification (no reply), and the unknown-method error path locks\ndown protocol correctness and guards against regressions in the JSON-RPC framing\nlayer.\n\nScope: test-only additions in src/lib.rs #[cfg(test)] using the existing\nframe_request / parse_framed_responses helpers. No production code changes\nexpected. Keep generic per crate constraints.\n\nFiled by mcp-cli-dev-aur-0 while progressing the lane (operator: workers improve\nprojects).\n","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-04T23:34:08.347579466Z","updated_at":"2026-06-04T23:36:27.544512023Z","closed_at":"2026-06-04T23:36:27.544511720Z","is_blocked":false,"spoken_name":"test mcp server paths","occurrence_count":1,"last_seen_at":"2026-06-04T23:34:08.347579466Z"}
{"id":"bd-1bd8b6","title":"MCP initialize should negotiate protocolVersion, not hardcode 2024-11-05","description":"Observed while implementing bd-909a82. In `McpServer::handle_request`, the `initialize` response hardcodes `\"protocolVersion\": \"2024-11-05\"` and ignores the `protocolVersion` the client sent in the initialize params. Per the MCP spec the server should echo back the client's requested protocol version when it can support it, and otherwise respond with a version it does support so the client can decide whether to proceed or disconnect. Hardcoding the oldest revision pins every session to 2024-11-05 even when a modern client (e.g. 2025-03-26 / 2025-06-18) connects; most clients will silently downgrade, but it is suboptimal and not spec-aligned negotiation.\n\nSuggested fix: read the client's requested `protocolVersion` from initialize params, and respond with that value if it is in a small set of supported versions, otherwise fall back to the server's latest supported version (currently 2024-11-05). Keep the crate generic — supported-version set can be a const slice. Add a test asserting a known client version is echoed and an unknown one falls back.\n\nFile: src/lib.rs, `initialize` arm of `handle_request`. Filed via reflect-session mixin from bd-909a82 session.","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-15T13:03:00.036597112Z","updated_at":"2026-06-16T00:25:29.623039112Z","closed_at":"2026-06-16T00:25:29.623037922Z","is_blocked":false,"spoken_name":"mcp protocol version negotiation","occurrence_count":1,"last_seen_at":"2026-06-15T13:03:00.036597112Z"}
{"id":"bd-3810a6","title":"Revert CI to rust-not-nix: plain cargo, drop nix develop","description":"Operator reversed nix-devshell CI plan (bd-02792a): nix cache = 5GB/job. Use plain cargo + dtolnay rust toolchain, drop nix develop. Keep azure-ephemeral + dispatch-only.","status":"closed","priority":2,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-28T23:04:07.863720152Z","updated_at":"2026-06-28T23:12:18.764601570Z","closed_at":"2026-06-28T23:12:18.764601183Z","is_blocked":false,"spoken_name":"revert ci to cargo","occurrence_count":1,"last_seen_at":"2026-06-28T23:04:07.863720152Z"}
{"id":"bd-5221f5","title":"Advertise tool outputSchema and optionally validate structuredContent (MCP 2025-06-18)","description":"MCP revision 2025-06-18 lets tools declare an `outputSchema` (JSON Schema for the tool's `structuredContent`) so clients can validate results. `mcp-cli` already returns `structuredContent` (the JsonEnvelope) from `tools/call`, but tool metadata (`ToolMetadata`) only carries `input_schema`; it does not advertise an output schema.\n\nPotential enhancement: generate and advertise an `outputSchema` for each typed tool so clients can validate structured results, and optionally validate outgoing `structuredContent` against it in debug/test builds.\n\nWhy this is a draft (needs maintainer/operator decision, not a unilateral change): generating an accurate output schema requires adding an `Output: JsonSchema` bound to `Tool::new_typed` / `add_typed_tool`. That is an additive-but-breaking API change — every consumer's output type would need to derive `JsonSchema`. The envelope wrapper (`JsonEnvelope<Output>`) also complicates what exactly the advertised schema should describe (the raw Output vs the full success/error envelope). Decide the scope (raw output schema vs envelope schema; opt-in vs always-on) before implementing.\n\nReferences: src/lib.rs `ToolMetadata`, `Tool::new_typed`, the `tools/call` arm of `handle_request`. Filed proactively during an idle burndown after closing bd-909a82 / bd-1bd8b6 / bd-e32020 / bd-b0314b.","status":"closed","priority":3,"type":"feature","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-16T02:11:27.082916338Z","updated_at":"2026-06-16T22:25:18.496542169Z","closed_at":"2026-06-16T22:25:18.496541891Z","is_blocked":false,"spoken_name":"declare output schema validation","occurrence_count":1,"last_seen_at":"2026-06-16T02:11:27.082916338Z"}
{"id":"bd-55de1d","title":"MCP server rejects JSON-RPC batches, but the advertised 2025-03-26 protocol version requires receiving them","description":"## Problem\n\n`SUPPORTED_PROTOCOL_VERSIONS` advertises `2025-03-26`, and that MCP revision\nrequires implementations to be able to RECEIVE JSON-RPC batches (an array of\nrequest objects). Sending batches was optional; receiving them was not.\n\nToday a batch array reaches `serde_json::from_value::<JsonRpcRequest>`, fails to\ndeserialize into a single request object, and is answered with a single\n`-32600 Invalid Request`. A spec-conformant `2025-03-26` client that batches its\n`initialize`/`tools/list` warm-up therefore gets one error instead of N\nresponses, and none of its calls run.\n\n## Expected (JSON-RPC 2.0 section 6)\n\n- A non-empty array is handled member-by-member; the server replies with an\n  array of the responses, in the same order, OMITTING members that are\n  notifications (no `id`).\n- A batch consisting only of notifications produces NO response at all.\n- An empty array is itself an Invalid Request and is answered with a single\n  `-32600` object, not an array.\n- A malformed member inside an otherwise valid batch gets its own `-32600`\n  entry; the sibling members are still executed.\n- The session keeps serving afterwards, consistent with the existing\n  reconnect-safe contract.\n\n## Notes\n\n`2025-06-18` removed batching. Accepting batches unconditionally is the tolerant\nchoice and keeps the code simpler than version-gating the transport; clients on\n`2025-06-18` simply never send them. Document that decision in the README.\n\n## Acceptance criteria\n\n- Batch requests are executed and answered as an array, ordered, notifications omitted.\n- Notification-only batches produce no output frame.\n- Empty batch returns a single `-32600`.\n- Framed stdio tests cover ordering, notification omission, notification-only,\n  empty batch, mixed valid/invalid members, and keep-serving.\n- README documents batch handling and the version rationale.\n- `cargo fmt` / `clippy -D warnings` / `cargo test` stay green.\n","status":"closed","priority":2,"type":"bug","creator":"ms-dev-3:mcp-cli:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","labels":["conformance","discovered-via-agent:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","discovered-via-node:ms-dev-3","discovered-via-session:self-improvement","mcp"],"dependencies":[],"created_at":"2026-07-30T15:25:08.178427204Z","updated_at":"2026-07-30T15:41:27.985747756Z","closed_at":"2026-07-30T15:41:27.985746918Z","is_blocked":false,"spoken_name":"mcp server batch support","last_claimed_at":"2026-07-30T15:25:14.668283534Z","closed_by":"ms-dev-3:mcp-cli:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","occurrence_count":1,"last_seen_at":"2026-07-30T15:25:08.178427204Z"}
{"id":"bd-7f4dd5","title":"Classify unknown-tool tools/call error as TargetNotFound, not Validation","description":"ToolRouter::call_tool returns an unknown-tool error with ErrorCategory::Validation (code \"unknown_tool\"). The ErrorCategory enum has a dedicated TargetNotFound variant that exists precisely for \"the named target does not exist\" — the same way a missing window id or file would be classified. Since the structured ErrorCategory is the framework's programmatic contract for routing/handling errors, a not-found condition surfacing as Validation (which is for malformed input) is a misclassification: consumers filtering on category cannot distinguish \"you sent bad input\" from \"that tool name doesn't exist\".\n\nFix: change the unknown-tool path in call_tool to ErrorCategory::TargetNotFound (code \"unknown_tool\" unchanged). The typed-input deserialization failure path stays Validation (that genuinely is bad input). Add a category assertion (target_not_found) to the existing stdio unknown-tool test. Pre-1.0 crate, so this small semantic refinement to error classification is acceptable. Run cargo fmt/clippy/test.","status":"closed","priority":3,"type":"bug","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-27T05:15:47.648074960Z","updated_at":"2026-06-27T05:21:02.804716343Z","closed_at":"2026-06-27T05:21:02.804715947Z","is_blocked":false,"spoken_name":"classify tool error target not found","occurrence_count":1,"last_seen_at":"2026-06-27T05:15:47.648074960Z"}
{"id":"bd-854d61","title":"MCP stdio: invalid UTF-8 on the transport ends the session (Io/InvalidData), same shape as the -32700 gap","description":"Observed while fixing bd-9171bc (malformed JSON frames now answered with -32700\ninstead of ending the session).\n\n`read_protocol_message` (src/lib.rs) frames each message with\n`reader.read_line(&mut line)` into a `String`. `BufRead::read_line` validates\nUTF-8 and returns `Err(io::ErrorKind::InvalidData)` on a non-UTF-8 byte. That\nerror propagates through `serve_transport` / `serve_stdio` as\n`McpCliError::Io` and terminates the session — the same \"one bad byte from the\npeer kills a long-lived server\" shape that bd-909a82 (-32600/-32602) and\nbd-9171bc (-32700) removed at the layers above, still live one layer down.\n\nMCP does require stdio frames to be UTF-8, so rejecting the frame is correct;\nending the session is the part that is not. A peer that emits one corrupt byte\n(binary noise, truncated multibyte write, a wrapper leaking non-UTF-8 log\noutput onto the same fd) loses every subsequent request.\n\nSuggested fix: read the frame as bytes (`read_until(b'\\n', &mut Vec<u8>)`),\nthen attempt UTF-8/JSON interpretation per frame. An invalid-UTF-8 frame\nbecomes a `-32700` parse error response (the id is unrecoverable, so null) and\nthe loop continues, exactly like the JSON case. Only genuine I/O failures on\nthe underlying reader should end the session.\n\nNote this reshapes the same read loop as bd-bb6800 (unbounded line length):\n`read_until` on a raw `Vec<u8>` is where a size cap would naturally live, so\nthe two are cheaper done together than separately.\n\nAcceptance: a non-UTF-8 frame yields -32700 and does not end the session; a\nvalid request after it is still served; genuine I/O errors still propagate;\nfmt/clippy/tests green.\n\nFiled via reflect-session mixin from the bd-9171bc session.\n","status":"closed","priority":3,"type":"task","creator":"ms-dev-4:mcp-cli:ms-dev-4-mcp-cli-mcp-cli-dev-md4-0","labels":[],"dependencies":[],"created_at":"2026-07-30T15:11:57.231322437Z","updated_at":"2026-07-30T15:32:46.517499940Z","closed_at":"2026-07-30T15:32:46.517499097Z","is_blocked":false,"spoken_name":"invalid utf-8 transport error","occurrence_count":1,"last_seen_at":"2026-07-30T15:11:57.231322437Z"}
{"id":"bd-870183","title":"tools/call structuredContent must conform to advertised outputSchema (envelope mismatch)","description":"Spec-correctness bug in the opt-in outputSchema feature (bd-5221f5). `tools/call` always returns `structuredContent` = the full JsonEnvelope serialization (`{status, meta, data}` on success / `{status, meta, error}` on error). But `new_typed_with_output_schema` advertises `outputSchema = schema_for!(Output)` — the schema of the bare `data` payload only. MCP 2025-06-18 requires that when a tool declares an outputSchema, the returned structuredContent MUST conform to it. A spec-compliant client validating structuredContent against the advertised outputSchema would FAIL, because the envelope's top-level shape (status/meta/data) does not match the bare Output schema.\n\nFix (non-breaking; structuredContent shape is unchanged so existing consumers like tendril are unaffected): advertise the schema of the envelope that actually wraps Output. Derive JsonSchema for ErrorCategory, EnvelopeMeta, JsonError, and JsonEnvelope<T>, and set `output_schema = schema_for!(JsonEnvelope<Output>)`. This makes the advertised schema describe the real structuredContent (a tagged success/error envelope whose `data` matches Output).\n\nUpdate the typed_tool_can_advertise_output_schema test to assert the envelope-shaped schema (status discriminator + data->Output), add coverage that a real tools/call structuredContent structurally matches the advertised schema, refresh the doc comment + README MCP section. Run cargo fmt/clippy/test.","status":"closed","priority":2,"type":"bug","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-27T04:22:22.482510796Z","updated_at":"2026-06-27T04:28:55.282905250Z","closed_at":"2026-06-27T04:28:55.282904867Z","is_blocked":false,"spoken_name":"tools call output schema mismatch","occurrence_count":1,"last_seen_at":"2026-06-27T04:22:22.482510796Z"}
{"id":"bd-8afbc4","title":"MCP stdio server tears down the session on invalid JSON instead of returning -32700 Parse error","description":"## Problem\n\n`McpServer::serve_transport` currently propagates a `McpCliError::Json` (and\ntherefore ENDS the serve loop) whenever a line on the stdio transport is not\nvalid JSON. A single stray non-JSON line — a wrapper process logging to stdout,\na partially flushed write, a shell banner, a UTF-8 BOM — kills the entire MCP\nsession, so the client sees the server exit rather than an error it can skip.\n\nThis is the last remaining teardown hole in the reconnect-safe contract:\nbd-909a82 already fixed malformed-but-parseable input (`-32600` Invalid Request,\n`-32602` Invalid params) and bd-b22230 locked idempotent re-initialize, but\n\"parseable as a line, not parseable as JSON\" still terminates the session.\n\nThe current behaviour is locked by the regression test\n`stdio_server_rejects_non_json_input_instead_of_hanging`, which asserts\n`Err(McpCliError::Json(_))`. That test's real intent was \"must not hang\"; the\nteardown was the expedient way to satisfy it at the time.\n\n## Expected\n\nJSON-RPC 2.0 section 5.1 defines `-32700 Parse error` for \"Invalid JSON was\nreceived by the server\", and MCP inherits it. The server should:\n\n1. Emit a `-32700` JSON-RPC error response with `id: null` (the id cannot be\n   recovered from unparseable text).\n2. KEEP SERVING subsequent well-formed messages on the same transport.\n\n## Acceptance criteria\n\n- Non-JSON input produces a `-32700` error response, not a terminated session.\n- A well-formed `tools/call` sent AFTER a garbage line is still answered.\n- The existing regression test is updated (not deleted) to assert the new\n  non-hanging, non-teardown contract.\n- README MCP protocol section documents `-32700` alongside the other codes.\n- `cargo fmt` / `clippy -D warnings` / `cargo test` stay green.\n","status":"closed","priority":2,"type":"bug","creator":"ms-dev-3:mcp-cli:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","labels":["discovered-via-agent:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","discovered-via-node:ms-dev-3","discovered-via-session:self-improvement","mcp","robustness"],"dependencies":[],"created_at":"2026-07-30T15:08:14.264309640Z","updated_at":"2026-07-30T15:22:04.095402723Z","closed_at":"2026-07-30T15:22:04.095401434Z","is_blocked":false,"spoken_name":"invalid json error handling","occurrence_count":1,"last_seen_at":"2026-07-30T15:08:14.264309640Z"}
{"id":"bd-909a82","title":"MCP stdio server should return JSON-RPC errors for invalid request / invalid params, not tear down the session","description":"A long-running MCP stdio server (McpServer::serve_transport) must keep serving across many requests on one connection. Today only `-32601 method not found` is handled gracefully; two other malformed-but-parseable cases instead propagate a Rust error out of `serve_transport`, terminating the entire session and dropping all subsequent valid requests:\n\n1. A JSON value that parses but is not a valid JSON-RPC request (e.g. missing `method`): `handle_request_value` does `serde_json::from_value::<JsonRpcRequest>(request)?` which propagates. Per JSON-RPC 2.0 this should be `-32600 Invalid Request` (id recovered from the raw value if present, else null) and the server should keep running.\n2. A `tools/call` with malformed `params` (e.g. missing `name`): `handle_request` does `serde_json::from_value::<ToolCallParams>(...)?` which propagates. Per JSON-RPC 2.0 this should be `-32602 Invalid params` with the request id, and the server should keep running.\n\nFix: handle both cases inside the request dispatch, returning the appropriate JSON-RPC error response object and continuing the serve loop. Add regression tests proving a session survives a bad message and still answers a following valid request.\n\nExplicit non-goal (respecting bd-c7aeca): the non-JSON transport line case (`serde_json::from_slice` in `serve_transport`, exercised by `stdio_server_rejects_non_json_input_instead_of_hanging`) was deliberately made to fail visibly rather than hang. This bead does not change that interactive-garbage behavior; it only fixes the parseable-JSON cases. Whether non-JSON lines should also become `-32700` + continue is left as a separate operator decision.","status":"closed","priority":2,"type":"bug","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-15T12:57:59.835752627Z","updated_at":"2026-06-15T13:06:11.356283590Z","closed_at":"2026-06-15T13:06:11.356283243Z","is_blocked":false,"spoken_name":"json rpc error handling","occurrence_count":1,"last_seen_at":"2026-06-15T12:57:59.835752627Z"}
{"id":"bd-9171bc","title":"MCP stdio: malformed JSON line kills the session instead of returning -32700 Parse error","description":"`McpServer::serve_transport` (src/lib.rs) parses each framed line with\n`serde_json::from_slice(&message)?`. A single line that is not valid JSON\ntherefore propagates `McpCliError::Json` out of `serve_transport` /\n`serve_stdio` and terminates the stdio session.\n\nPer JSON-RPC 2.0 (and the MCP stdio transport, which is newline-delimited\nJSON-RPC), invalid JSON must be answered with a `-32700 Parse error`\nresponse carrying `\"id\": null`, and a long-lived server should keep serving\nsubsequent messages.\n\nThis is the same defect class as the closed bd-909a82, which made invalid\n*request objects* (`-32600`) and invalid *params* (`-32602`) non-fatal and\nkept the session alive. The parse-error leg was never covered, so the layer\ndirectly beneath those two fixes still tears the session down. One bad byte\nfrom a buggy or noisy client kills an otherwise healthy server.\n\nCurrent behaviour is pinned by the test\n`stdio_server_rejects_non_json_input_instead_of_hanging` (src/lib.rs ~1373),\nwhich asserts `Err(McpCliError::Json(_))`. That test was written to fix an\nearlier hang; the error-return was an improvement over hanging but is not the\nprotocol-conformant outcome. It should be re-pointed at the `-32700` contract\nwhile keeping the original \"must not hang / must not silently consume\"\nproperty.\n\nAcceptance:\n- a malformed JSON line yields a `-32700` JSON-RPC error response with\n  `\"id\": null` and does NOT end the session;\n- a valid request following a malformed line is still served;\n- the existing non-JSON regression test is updated (not deleted) to the new\n  contract;\n- fmt/clippy/tests green.\n","status":"closed","priority":2,"type":"bug","creator":"ms-dev-4:mcp-cli:ms-dev-4-mcp-cli-mcp-cli-dev-md4-0","labels":[],"dependencies":[],"created_at":"2026-07-30T15:09:34.252460265Z","updated_at":"2026-07-30T15:14:28.525333518Z","closed_at":"2026-07-30T15:14:28.525332906Z","is_blocked":false,"spoken_name":"stdio parse error handling","occurrence_count":1,"last_seen_at":"2026-07-30T15:09:34.252460265Z"}
{"id":"bd-9e5da2","title":"Cover JsonEnvelope round-trip deserialize and initialize handshake completeness","description":"Two public-contract behaviors lack test coverage:\n\n1. JsonEnvelope<T> round-trip deserialize. The envelope is a #[serde(tag=\"status\")]\n   tagged enum deriving Deserialize and is the crate's primary --json wire contract\n   that consumers parse. Today only the serialize direction is tested; nothing proves\n   the success and error variants deserialize back from their serialized JSON form\n   (status tag dispatch, data/error payloads, meta).\n\n2. initialize response completeness. The stdio initialize handler emits\n   protocolVersion (\"2024-11-05\"), capabilities.tools.listChanged (false), and\n   serverInfo.version (from StdioServerConfig.server_version), but the existing test\n   only asserts serverInfo.name. A small assertion set locks the handshake shape.\n\nScope: test-only additions in src/lib.rs #[cfg(test)]. No production changes. Keep\ngeneric. Locks the public CLI envelope contract bidirectionally and the MCP\ninitialize handshake.\n\nFiled by mcp-cli-dev-aur-0 while progressing the lane.\n","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-05T00:14:10.383429621Z","updated_at":"2026-06-05T00:17:06.317265721Z","closed_at":"2026-06-05T00:17:06.317265352Z","is_blocked":false,"spoken_name":"test json envelope deserialization","occurrence_count":1,"last_seen_at":"2026-06-05T00:14:10.383429621Z"}
{"id":"bd-b0314b","title":"Document the MCP protocol surface (methods, version negotiation, error codes) in README","description":"The README documents JSON envelopes and tool registration but never describes the MCP stdio protocol surface the crate actually implements. Recent changes added public/behavioral surface that should be documented per the project profile's \"Update README.md when public APIs or integration patterns change\" rule:\n\n- newline-delimited JSON (NDJSON) stdio framing (bd-c7aeca)\n- supported MCP methods: initialize (with protocol-version negotiation), ping, tools/list, tools/call, notifications/initialized\n- the new pub const SUPPORTED_PROTOCOL_VERSIONS and negotiation behavior (bd-1bd8b6)\n- JSON-RPC error behavior that keeps the session alive: -32600 invalid request, -32601 method not found, -32602 invalid params (bd-909a82)\n\nAdd a concise \"MCP protocol support\" section to README.md covering these. Keep it generic (no consuming-project specifics). No code change; run fmt/clippy/test to confirm nothing regresses.","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-16T01:39:42.492262908Z","updated_at":"2026-06-16T01:42:07.458161681Z","closed_at":"2026-06-16T01:42:07.458161372Z","is_blocked":false,"spoken_name":"document mcp protocol","occurrence_count":1,"last_seen_at":"2026-06-16T01:39:42.492262908Z"}
{"id":"bd-b22230","title":"Lock MCP stdio reconnect-safe contract: idempotent repeated initialize keeps serving","description":"Operator (Harry, 2026-06-24) raised that a same-agent MCP reconnect can fail to be re-acknowledged in the cacophony precursor binary. Investigation confirms mcp-cli's McpServer is stateless per-message (holds only config+router; no per-connection/'already-initialized' state), so a repeated `initialize` on the same stdio stream is already answered idempotently and the session keeps serving. This bead locks that reconnect-safe contract with explicit regression tests so it stays the reference behavior the precursor should match:\n\n1. Two `initialize` calls on the same serve_transport session both return a well-formed result (serverInfo + protocolVersion) and the session continues serving subsequent requests (e.g. a following ping/tools-list answered).\n2. A re-`initialize` after `notifications/initialized` is still answered (no 'already initialized' rejection).\n\nNo production code change expected (documents/locks existing behavior); pure additive tests. Run cargo fmt/clippy/test.","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-26T22:52:34.133353678Z","updated_at":"2026-06-27T00:33:45.267541336Z","closed_at":"2026-06-27T00:33:45.267541074Z","is_blocked":false,"spoken_name":"lock mcp reconnect safe contract","occurrence_count":1,"last_seen_at":"2026-06-26T22:52:34.133353678Z"}
{"id":"bd-b5b793","title":"Switch mcp-cli reintegration to PR auto-merge (CI gate now live)","description":"Harry directed (2026-06-29, via sgu24:nix-on-droid): switch to PR mode rather than relying on flaky direct checkouts; prefer PR auto-merge when available. mcp-cli now qualifies: hosted CI gate is green on push+PR (bd-bcb4b8), legs 1+2 enabled, and branch protection on main now requires the 'check' context (strict, enforce_admins=false escape hatch). Update the profile so pr_auto_merge is the default reintegration mode (direct kept as documented escape hatch). Dogfood by landing this very change via --mode pr_auto_merge.","status":"closed","priority":2,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-29T19:40:02.049919054Z","updated_at":"2026-06-29T19:48:10.324159848Z","closed_at":"2026-06-29T19:48:10.324159349Z","is_blocked":false,"spoken_name":"enable pr auto merge","occurrence_count":1,"last_seen_at":"2026-06-29T19:40:02.049919054Z"}
{"id":"bd-bb6800","title":"Harden read_protocol_message against unbounded line length","description":"Observed during a reflect-session audit of src/lib.rs (post-burndown, board drained).\n\n`read_protocol_message` (src/lib.rs) frames NDJSON by calling `reader.read_line(&mut line)` in a loop with no upper bound on line length. A single pathological/oversized line (no embedded newline) would be buffered entirely in memory, an unbounded allocation. Today the stdin peer is the agent's own trusted MCP client, so the practical risk is low and this is hardening, not a live bug.\n\nSuggested fix: cap the accepted frame size with a configurable/default max (e.g. read via `reader.take(MAX).read_line(...)` or a manual byte loop), and on overflow return a structured framing error (McpCliError::Protocol or a JSON-RPC error that keeps the session serving, consistent with the existing invalid-request/-32600 handling) rather than allocating without limit. Add a test that a frame exceeding the cap is rejected cleanly and the session keeps serving the next valid request.\n\nAcceptance: bounded read with a sensible default cap; clean rejection (no panic, no session teardown) on overflow; unit test covering the overflow path; clippy/fmt/tests stay green.\n\nFiled via reflect-session mixin from the mcp-cli post-burndown idle audit (no anchor bead; board was fully drained).","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-30T20:37:38.053781016Z","updated_at":"2026-07-30T15:32:30.234405396Z","closed_at":"2026-07-30T15:32:30.234404645Z","is_blocked":false,"spoken_name":"bound line length","occurrence_count":1,"last_seen_at":"2026-06-30T20:37:38.053781016Z"}
{"id":"bd-bcb4b8","title":"CI: GitHub-hosted cargo gate on pull_request (complete auto-merge cutover leg 3)","description":"Harry's auto-merge cutover: legs 1+2 (allow_auto_merge + allow_update_branch) already enabled on harryaskham/mcp-cli. Merge queue needs a GitHub org (mcp-cli is a personal-account repo) so it's unavailable for now — proceed with the tools we have. Leg 3 = a required GREEN CI check so auto-merge gates. mcp-cli CI was workflow_dispatch-only targeting a self-hosted azure-ephemeral pool (0 runners). Repo is public, so switch the CI job to GitHub-hosted ubuntu-latest and trigger on pull_request + push to main (also merge_group for future MQ). Keep the existing cargo fmt/clippy/test validation (rust-not-nix via dtolnay). After it lands + runs once, require the check context in branch protection (strict). Operator green-lit proceeding.","status":"closed","priority":2,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-29T16:43:43.075455644Z","updated_at":"2026-06-29T16:51:03.601805362Z","closed_at":"2026-06-29T16:51:03.601804888Z","is_blocked":false,"spoken_name":"github ci pull request gate","occurrence_count":1,"last_seen_at":"2026-06-29T16:43:43.075455644Z"}
{"id":"bd-bfebfc","title":"Switch mcp-cli dev reintegration to PR + CI gate (drop dispatch-only)","description":"Per Harry broadcast 2026-06-29: dev re-ints use PR mode; local gate dropped; GitHub Action keeps main green. Enable push/pull_request triggers in .github/workflows/ci.yml (was dispatch-only pending runners) and note PR-mode reintegration in profile. One agent only; no swarm.","status":"closed","priority":1,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-29T01:04:30.198983500Z","updated_at":"2026-06-29T01:09:26.691849150Z","closed_at":"2026-06-29T01:09:26.691848602Z","is_blocked":false,"spoken_name":"enable pr ci gate","occurrence_count":1,"last_seen_at":"2026-06-29T01:04:30.198983500Z"}
{"id":"bd-c7aeca","title":"stdio transport must use newline-delimited JSON (MCP), not Content-Length framing","description":"mcp-cli's stdio transport uses LSP-style Content-Length header framing\n(read_protocol_message / write_protocol_message), but the MCP stdio transport\nspecification uses newline-delimited JSON (NDJSON): each JSON-RPC message is a\nsingle line terminated by \\n, with no framing headers and no embedded newlines.\n\nImpact: any real MCP client (e.g. `tendril mcp` / `tendril mcp stdio`) sends an\ninitialize request as a single newline-terminated JSON line with NO\nContent-Length header. The server's read loop treats that JSON as a header line,\nnever finds the expected Content-Length + blank-line terminator, and blocks\nreading more \"headers\" — so it never parses the request and never responds. The\nclient hangs in \"connecting\" forever. Operator reports all mcp-cli apps get\nstuck connecting under tendril mcp.\n\nRoot cause masked by tests: every test (frame_request / parse_framed_responses)\nspeaks the server's own Content-Length dialect, so all 19 pass while real MCP\nclients fail.\n\nFix: replace Content-Length framing with newline-delimited JSON on both read and\nwrite paths:\n- read: read one line, trim trailing \\r\\n, skip blank lines, parse the line as a\n  full JSON-RPC message; EOF -> None.\n- write: serialize compact JSON (no embedded newlines) + a single trailing \\n,\n  flush.\n- Update test helpers and assertions to NDJSON. Keep the public serve_stdio /\n  serve_transport signatures stable.\n\nScope: src/lib.rs production framing + tests. P1-ish correctness bug but filed as\ntask P2.\n","status":"closed","priority":2,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-06T14:03:46.114395992Z","updated_at":"2026-06-06T14:31:00.146161693Z","closed_at":"2026-06-06T14:31:00.146160073Z","is_blocked":false,"spoken_name":"fix stdio transport ndjson","occurrence_count":1,"last_seen_at":"2026-06-06T14:03:46.114395992Z"}
{"id":"bd-d653db","title":"Cover stdio tools/call error path (isError=true) end-to-end","description":"The stdio tools/call handler returns isError=true with the structured error\nenvelope embedded in structuredContent when a tool call fails, but only the\nsuccess path (isError=false) is tested today\n(stdio_server_handles_initialize_list_and_call asserts isError==false).\n\nThe error half of the most important MCP operation is uncovered end-to-end\nthrough the framing layer:\n\n- tools/call for an unknown tool name -> isError=true, structuredContent.status\n  == \"error\", error.code == \"unknown_tool\" (router emits \"unknown tool `name`\").\n- tools/call with arguments that fail typed validation -> isError=true,\n  structuredContent carries the validation error category/code.\n\nA regression here would silently stop surfacing errors to MCP clients. Adding a\nfocused stdio test locks isError propagation and the embedded error envelope shape.\n\nScope: test-only additions in src/lib.rs #[cfg(test)] using existing\nframe_request/parse_framed_responses helpers and build_math_router. No production\nchanges. Keep generic.\n\nFiled by mcp-cli-dev-aur-0 while progressing the lane.\n","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-05T00:33:49.518885812Z","updated_at":"2026-06-05T00:35:59.279144048Z","closed_at":"2026-06-05T00:35:59.279143409Z","is_blocked":false,"spoken_name":"test stdio call errors","occurrence_count":1,"last_seen_at":"2026-06-05T00:33:49.518885812Z"}
{"id":"bd-e32020","title":"McpCliError::category() always returns SerializationError for every variant","description":"`McpCliError::category()` (src/lib.rs) unconditionally returns `ErrorCategory::SerializationError` for every variant, including `Io(io::Error)` and `Protocol(String)`. Any consumer that projects a transport/CLI error into a JsonError via this category therefore mislabels I/O failures and protocol violations as serialization errors.\n\nFix: match on the variant and map to an appropriate category:\n- `Io(_)` -> `ExecutionFailure` (transport I/O failure)\n- `Json(_)` -> `SerializationError`\n- `Protocol(_)` -> `Validation` (protocol/message violated expectations)\n\nKeep the method a `const fn` (match is const-fn-legal). Add a unit test asserting each variant maps to its category.","status":"closed","priority":3,"type":"bug","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-16T00:42:00.815299204Z","updated_at":"2026-06-16T00:45:42.330072117Z","closed_at":"2026-06-16T00:45:42.330071131Z","is_blocked":false,"spoken_name":"fix error category mapping","occurrence_count":1,"last_seen_at":"2026-06-16T00:42:00.815299204Z"}
{"id":"bd-f023df","title":"Add GitHub Actions CI on azure-ephemeral self-hosted runner","description":"Operator (Harry, 2026-06-28) directive: bring every project's CI onto the new azure-ephemeral self-hosted GitHub runners via `runs-on: [self-hosted, azure-ephemeral]`; one agent per project applies it. mcp-cli currently has NO CI at all (no .github/ directory), so this establishes minimal CI from scratch rather than retagging existing workflows.\n\nAdd `.github/workflows/ci.yml` with a single `check` job on `runs-on: [self-hosted, azure-ephemeral]`, triggered on push + pull_request to main, that mirrors the crate's existing local validation: cargo fmt --all -- --check; cargo clippy --workspace --all-targets --all-features -- -D warnings; cargo test --workspace --all-features. Use dtolnay/rust-toolchain@stable (components rustfmt, clippy) so the runner has the toolchain regardless of image (edition 2024 / MSRV 1.85). Validate locally with fmt/clippy/test (adding the workflow file does not affect the cargo build).","status":"closed","priority":2,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-28T17:41:13.625162764Z","updated_at":"2026-06-28T19:14:50.009791451Z","closed_at":"2026-06-28T19:14:50.009791120Z","is_blocked":false,"spoken_name":"add github actions ci","occurrence_count":1,"last_seen_at":"2026-06-28T17:41:13.625162764Z"}
{"id":"bd-f192d4","title":"Add runnable doctest for README minimal pattern (0 doctests today)","description":"The README \"Minimal pattern\" section shows a `rust` fenced code block demonstrating\nToolRouter + McpServer usage, but it is not compiled or run as a doctest. `cargo test\n--workspace --all-features` reports `Doc-tests mcp_cli ... 0 tests`, so the public\nexample is never verified against the real API and can silently rot as the surface\nevolves.\n\nSuggested fix shape: add a crate-level `//!` doc comment in `src/lib.rs` containing the\nsame minimal example as a runnable doctest (mirroring the README), or convert the README\nblock into a tested example. Keep the example generic per the crate's design constraint.\nThis gives compile-time protection for the documented entry-point pattern.\n\nObserved during autonomous health-check of mcp-cli (check/test/clippy all green, 7 unit\ntests pass, 0 doctests). Filed via reflect-session mixin from mcp-cli-dev-aur-0 idle\nsession.\n","status":"closed","priority":3,"type":"task","creator":"aurora:mcp-cli:aurora-mcp-cli-mcp-cli-dev-aur-0","labels":[],"dependencies":[],"created_at":"2026-06-03T21:06:15.796452389Z","updated_at":"2026-06-04T23:16:57.277437569Z","closed_at":"2026-06-04T23:16:57.277436737Z","is_blocked":false,"spoken_name":"add doctest readme example","occurrence_count":1,"last_seen_at":"2026-06-03T21:06:15.796452389Z"}
{"id":"mut-019fb3b0-f870-70f1-a42e-724b0ca164b2","ts":"2026-07-30T15:42:27.440859695Z","bead_id":"bd-0f31f3","action":"create","actor":"ms-dev-3:mcp-cli:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","bead":{"id":"bd-0f31f3","title":"Drop the unused tracing dependency (never referenced since the crate extraction)","description":"## Problem\n\n`Cargo.toml` declares `tracing = \"0.1.41\"`, but `src/lib.rs` contains zero\nreferences to it. The dependency was introduced in the extraction commit\n(bd-f9bf4d, `9e2f1fc`) and has never been used, so every consumer of `mcp-cli`\ninherits `tracing`, `tracing-core`, and `pin-project-lite` for nothing.\n\nVerify with:\n\n    grep -c 'tracing' src/lib.rs   # 0\n\n## Options\n\n1. **Remove it** (preferred, matches the crate's \"keep it minimal\" design rule).\n   Zero risk: the compiler proves nothing uses it, and at 0.0.1 there is no\n   semver obligation.\n2. Or deliberately wire it up. An MCP stdio server cannot log to stdout (stdout\n   is the protocol channel), so `tracing` events on the protocol error paths\n   (parse error, invalid request, unknown method, tool failure) would be the\n   idiomatic way to give consumers observability. This is a bigger, opinionated\n   change and should be its own bead with its own tests if wanted.\n\nUnless someone wants option 2, take option 1.\n\n## Acceptance criteria\n\n- `tracing` removed from `Cargo.toml` (or used, if option 2 is chosen deliberately).\n- `cargo fmt` / `clippy -D warnings` / `cargo test` stay green.\n","status":"open","priority":3,"type":"task","creator":"ms-dev-3:mcp-cli:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","labels":["hygiene","dependencies","discovered-via-agent:ms-dev-3-mcp-cli-mcp-cli-dev-md3-0","discovered-via-session:self-improvement","discovered-via-node:ms-dev-3"],"dependencies":[],"created_at":"2026-07-30T15:42:27.440842144Z","updated_at":"2026-07-30T15:42:27.440842144Z","is_blocked":false,"spoken_name":"Drop-Unused","occurrence_count":1,"last_seen_at":"2026-07-30T15:42:27.440842144Z"}}
